sudo: required
language: go

go_import_path: github.com/kubemove/kubemove

services:
  - docker
language: go
go:
  - 1.13.8

addons:
  apt:
    update: true

script:
  - make vet
  - make golint
  - make
  - make images
  - make deploy-images
